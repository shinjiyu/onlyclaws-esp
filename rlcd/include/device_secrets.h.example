#pragma once

// Copy to device_secrets.h (gitignored) and fill token.
// Cloud host defaults live in cloud_config.h → onlyclaws.world/epaper

#ifndef EPD_DEVICE_ID
#define EPD_DEVICE_ID "a4cb8fdf8440"
#endif

#ifndef EPD_DEVICE_TOKEN
#define EPD_DEVICE_TOKEN "your_device_token"
#endif

// Optional overrides (else cloud_config.h / NVS):
// #define EPD_API_HOST "onlyclaws.world"
